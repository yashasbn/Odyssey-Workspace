# Odyssey-Workspace

A local AI chat interface powered by Ollama running in Docker.

## Overview
This workspace contains a lightweight, zero-leak chat interface that connects directly to your local Ollama instance running inside a Docker container.
